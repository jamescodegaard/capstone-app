{
  event_id: event.id,
  name: event.name,
  date: event.date,
  description: event.description,
  contact_person: event.alt_contact,
  contact_email: event.alt_email,
  business: event.business.name,
  image: event.image,
}